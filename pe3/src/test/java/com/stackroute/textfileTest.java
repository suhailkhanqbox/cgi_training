package com.stackroute;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;
/*
public class textfileTest {
    private textfile t1=new textfile();
    @Before
    public void setUp() throws Exception {
    }

    @After
    public void tearDown() throws Exception {
    }

    @Test
    public void testcase1()
    {
        String to_write="random text in the file just to check test case";

       String []s = t1.fileRead(to_write);
        assertEquals("passed VALue has capital string and file size in array",["RANDOM TEXT IN THE FILE JUST TO CHECK TEST CASE","47"],s);
        assertNotEquals("passed VALue has capital string and file size in array",["RANDOM TEXT IN THE FILE JUST To CHECK TEST CASE","42"],s);
    }
}

 */